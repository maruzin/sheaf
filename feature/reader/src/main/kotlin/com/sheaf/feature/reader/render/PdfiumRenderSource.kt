package com.sheaf.feature.reader.render

// PDFium renderer removed — reverted to the platform PdfRenderer (see BUILD_NOTES).
// PDFium 2.x requires Kotlin 2.3 + compileSdk 36; deferred behind a toolchain upgrade.
